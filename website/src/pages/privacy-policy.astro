---
import "@/styles/terms-and-privacy.css";

import Layout from "@/components/layout/Layout.astro";
import { createTranslator, getLocaleFromUrl } from "@/utils/i18n";
import { createMetadata } from "@/utils/create-metadata";
import { JsonLdGraph } from "@/utils/build-json-ld";
import {
  androidAppJsonLd,
  iosAppJsonLd,
  organizationJsonLd,
  websiteJsonLd,
} from "@/constants/default-jsonld";

const locale = getLocaleFromUrl(Astro.url.pathname);
const t = await createTranslator(locale);

const metadata = createMetadata({
  title: t("v2.privacy.meta.title"),
  description: t("v2.privacy.meta.description"),
  pathname: Astro.url.pathname,
});

const jsonLd = JsonLdGraph({
  "@context": "https://schema.org",
  "@graph": [websiteJsonLd, organizationJsonLd, iosAppJsonLd, androidAppJsonLd],
});

const infoCollectWebsiteItems = t(
  "v2.privacy.information_collect.from_website_items",
) as string[];
const infoCollectFromYouItems = t(
  "v2.privacy.information_collect.from_you_items",
) as string[];
const infoCollectTechnicalItems = t(
  "v2.privacy.information_collect.technical_items",
) as string[];

const howWeUseItems = t("v2.privacy.how_we_use.items") as string[];
const howWeUseDoNotItems = t(
  "v2.privacy.how_we_use.we_do_not_items",
) as string[];

const infoSharingItems = t("v2.privacy.information_sharing.items") as string[];

const cookiesItems = t("v2.privacy.cookies_tracking.items") as string[];

const dataSecurityRetentionItems = t(
  "v2.privacy.data_security.retention_items",
) as string[];

const additionalInfoRegulatoryItems = t(
  "v2.privacy.additional_info.regulatory_items",
) as string[];
---

<Layout {...metadata} jsonLd={jsonLd}>
  <!-- HERO -->
  <section id="policy-hero">
    <span class="policy-eyebrow">
      {t("v2.privacy.hero.eyebrow")}
    </span>
    <h1 class="policy-h1">
      {t("v2.privacy.hero.title")}
    </h1>
    <span class="policy-meta">
      {t("v2.privacy.hero.last_updated")}
    </span>
    <div class="policy-divider"></div>
  </section>

  <!-- CONTENT -->
  <section id="policy-content">
    <span class="policy-sub-h3">
      {t("v2.privacy.information_collect.title")}
    </span>

    <p class="policy-body">
      {t("v2.privacy.information_collect.from_apps")}{" "}
      {t("v2.privacy.information_collect.from_apps_content")}
    </p>

    <p class="policy-body">
      {t("v2.privacy.information_collect.from_website")}
    </p>
    <ul class="policy-list">
      {infoCollectWebsiteItems.map((item) => <li>{item}</li>)}
    </ul>

    <p class="policy-body">
      {t("v2.privacy.information_collect.from_you")}
    </p>
    <ul class="policy-list">
      {infoCollectFromYouItems.map((item) => <li>{item}</li>)}
    </ul>

    <p class="policy-body">
      {t("v2.privacy.information_collect.technical")}
    </p>
    <ul class="policy-list">
      {infoCollectTechnicalItems.map((item) => <li>{item}</li>)}
    </ul>

    <span class="policy-sub-h3">
      {t("v2.privacy.how_we_use.title")}
    </span>

    <p class="policy-body">
      {t("v2.privacy.how_we_use.intro")}
    </p>
    <ul class="policy-list">
      {howWeUseItems.map((item) => <li>{item}</li>)}
    </ul>

    <p class="policy-body">
      {t("v2.privacy.how_we_use.we_do_not")}
    </p>
    <ul class="policy-list">
      {howWeUseDoNotItems.map((item) => <li>{item}</li>)}
    </ul>

    <span class="policy-sub-h3">
      {t("v2.privacy.information_sharing.title")}
    </span>

    <p class="policy-body">
      {t("v2.privacy.information_sharing.intro")}
    </p>
    <ul class="policy-list">
      {infoSharingItems.map((item) => <li>{item}</li>)}
    </ul>
    <p class="policy-body">
      {t("v2.privacy.information_sharing.note")}
    </p>

    <span class="policy-sub-h3">
      {t("v2.privacy.cookies_tracking.title")}
    </span>

    <p class="policy-body">
      {t("v2.privacy.cookies_tracking.intro")}
    </p>
    <ul class="policy-list">
      {cookiesItems.map((item) => <li>{item}</li>)}
    </ul>
    <p class="policy-body">
      {t("v2.privacy.cookies_tracking.note")}
    </p>

    <span class="policy-sub-h3">
      {t("v2.privacy.data_security.title")}
    </span>

    <p class="policy-body">
      {t("v2.privacy.data_security.intro")}
    </p>

    <p class="policy-body">
      {t("v2.privacy.data_security.technical")}
    </p>
    <ul class="policy-list">
      {dataSecurityRetentionItems.map((item) => <li>{item}</li>)}
    </ul>

    <span class="policy-sub-h3">
      {t("v2.privacy.your_rights.title")}
    </span>

    <p class="policy-body">
      {t("v2.privacy.your_rights.general")}
    </p>

    <p class="policy-body">
      {t("v2.privacy.your_rights.california")}
    </p>

    <p class="policy-body">
      {t("v2.privacy.your_rights.eu_uk")}
    </p>

    <span class="policy-sub-h3">
      {t("v2.privacy.international_transfers.title")}
    </span>

    <p class="policy-body">
      {t("v2.privacy.international_transfers.content")}
    </p>

    <span class="policy-sub-h3">
      {t("v2.privacy.childrens_privacy.title")}
    </span>

    <p class="policy-body">
      {t("v2.privacy.childrens_privacy.content")}
    </p>

    <span class="policy-sub-h3">
      {t("v2.privacy.additional_info.title")}
    </span>

    <p class="policy-body">
      {t("v2.privacy.additional_info.regulatory")}
    </p>
    <ul class="policy-list">
      {additionalInfoRegulatoryItems.map((item) => <li>{item}</li>)}
    </ul>

    <span class="policy-sub-h3">
      {t("v2.privacy.contact_info.title")}
    </span>

    <p class="policy-body">
      {
        (t("v2.privacy.contact_info.address") as string)
          .split("\n")
          .map((line, idx, arr) => (
            <>
              {line}
              {idx < arr.length - 1 && <br />}
            </>
          ))
      }
    </p>
    <p class="policy-body">
      {t("v2.privacy.contact_info.note")}{" "}
      <a href="/contact/" style="color:#FF6B35;text-decoration:none;">
        {t("v2.privacy.contact_info.link")}
      </a>
    </p>
  </section>
</Layout>
