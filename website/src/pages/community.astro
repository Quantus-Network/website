---
import Layout from "@/components/layout/Layout.astro";
import { createTranslator, getLocaleFromUrl } from "@/utils/i18n";
import { createMetadata } from "@/utils/create-metadata";
import { JsonLdGraph } from "@/utils/build-json-ld";
import {
  androidAppJsonLd,
  iosAppJsonLd,
  organizationJsonLd,
  websiteJsonLd,
} from "@/constants/default-jsonld";
import HeroBanner from "@/components/features/community/HeroBanner.astro";
import EventSection from "@/components/features/community/EventSection.astro";
import PodcastSection from "@/components/features/community/PodcastSection.astro";
import BlogSection from "@/components/features/community/BlogSection.astro";
import TelegramSection from "@/components/features/community/TelegramSection.astro";
import SocialsSection from "@/components/features/community/SocialsSection.astro";

const locale = getLocaleFromUrl(Astro.url.pathname);
const t = await createTranslator(locale);

const metadata = createMetadata({
  title: t("community.meta.title"),
  description: t("community.meta.description"),
  pathname: Astro.url.pathname,
});

const jsonLd = JsonLdGraph({
  "@context": "https://schema.org",
  "@graph": [websiteJsonLd, organizationJsonLd, iosAppJsonLd, androidAppJsonLd],
});
---

<Layout {...metadata} jsonLd={jsonLd}>
  <HeroBanner />
  <EventSection />
  <PodcastSection />
  <BlogSection />
  <TelegramSection />
  <SocialsSection />
</Layout>
