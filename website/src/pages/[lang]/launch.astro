---
import LaunchPage from "@/components/features/launch/LaunchPage.astro";
import { PREFIXED_LOCALES } from "@/utils/i18n";

export async function getStaticPaths() {
  return PREFIXED_LOCALES.map((lang) => ({ params: { lang } }));
}
---

<LaunchPage />
