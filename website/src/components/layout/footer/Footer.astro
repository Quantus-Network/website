---
import { FOOTER_NAVIGATIONS } from "@/constants/footer-navigations";
import { applyStyles } from "@/utils/apply-styles";
import {
  createTranslator,
  getLocaleFromUrl,
  getLocalizedPath,
} from "@/utils/i18n";
import LanguageSwitcher from "@/components/ui/LanguageSwitcher.astro";

const locale = getLocaleFromUrl(Astro.url.pathname);
const t = await createTranslator(locale);

const year = new Date().getFullYear();

const currentPath = Astro.url.pathname;
const isCurrentPage = (href: string) =>
  currentPath === getLocalizedPath(locale, href);
---

<footer
  id="footer"
  class="border-content-8 bg-void relative z-50 grid min-h-[calc(100vh-66px)] grid-cols-1 overflow-hidden border-t md:grid-cols-2"
>
  <!-- Background Glows -->
  <div
    class="pointer-events-none absolute -bottom-[100px] -left-[100px] z-0 h-[500px] w-[500px] bg-[radial-gradient(circle,var(--color-flare-12)_0%,transparent_70%)]"
  >
  </div>
  <div
    class="pointer-events-none absolute -top-[80px] -right-[80px] z-0 h-[450px] w-[450px] bg-[radial-gradient(circle,var(--color-flare-8)_0%,transparent_70%)]"
  >
  </div>

  <!-- Left Column -->
  <div
    class="border-content-8 p-content-edges-mobile md:p-content-edges relative z-10 flex flex-col justify-between border-b md:border-r md:border-b-0"
  >
    <a
      href={getLocalizedPath(locale, "/")}
      class="text-content-12 font-primary text-[clamp(32px,10vw,56px)] leading-none font-medium -tracking-[0.02em] no-underline md:text-[clamp(48px,6vw,80px)]"
    >
      {t("footer.brand")}
    </a>
    <div class="mt-8 flex flex-col gap-4 md:mt-0">
      <LanguageSwitcher />
      <div class="flex flex-col gap-2">
        <span class="text-content-25 font-mono text-xs tracking-[0.08em]">
          {t("footer.copyright", { year })}
        </span>
        <span class="text-content-25 font-mono text-xs tracking-[0.08em]">
          {t("footer.rights")}
        </span>
      </div>
    </div>
  </div>

  <!-- Right Column -->
  <div
    class="p-content-edges-mobile md:p-content-edges relative z-10 flex flex-col justify-between"
  >
    <div class="grid grid-cols-2 gap-8 md:grid-cols-3 md:gap-10">
      {
        FOOTER_NAVIGATIONS.map((section) => (
          <div class="flex flex-col">
            <span class="text-content-30 mb-6 block font-mono text-xs tracking-[0.16em] uppercase">
              {t(section.label)}
            </span>
            {section.children.map((link) => (
              <a
                href={
                  link.href.startsWith("http")
                    ? link.href
                    : getLocalizedPath(locale, link.href)
                }
                target={link.target}
                rel={link.rel}
                class={applyStyles(
                  "text-content-55 hover:text-content-90 mb-3.5 block text-[13px] no-underline transition-colors duration-200",
                  isCurrentPage(link.href) && "text-content-90",
                )}
              >
                {t(link.label)}
              </a>
            ))}
          </div>
        ))
      }
    </div>
    <div class="mt-10 flex flex-col gap-1 md:mt-0">
      <span class="text-content-20 font-mono text-xs tracking-[0.12em]">
        {t("footer.built_on")}
      </span>
    </div>
  </div>
</footer>
